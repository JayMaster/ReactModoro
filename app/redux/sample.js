const initialState = {
	willFight: false,
	myNumber: false,
}

export default function sample (state = initialState, action) { // if state = undefined, make it initialState
	switch (action.type) {

		default : 
			return state
	}
}